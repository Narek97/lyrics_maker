import styled from 'styled-components'
import { AUDIO_PANEL_HEIGHT } from '../../constants'

export const Container = styled.div`
  display: flex;
  width: 100%;
  height: ${AUDIO_PANEL_HEIGHT}px;
  border-top: 1px solid #dde3f0;
  background-color: rgb(255, 255, 255);
`
